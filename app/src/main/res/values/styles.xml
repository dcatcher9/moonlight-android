<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.

    -->
    <style name="AppBaseTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/xr_surface_sunken</item>
        <item name="android:navigationBarColor">@color/xr_surface_sunken</item>
        <item name="android:colorBackground">@color/xr_surface_sunken</item>
        <item name="android:windowBackground">@color/xr_surface_sunken</item>
    </style>

    <style name="StreamBaseTheme" parent="AppBaseTheme">
        <!-- Transparent streaming background to avoid extra overdraw -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Stream activity theme -->
    <style name="StreamTheme" parent="StreamBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- Added for API 34+ where ImageButton requires these color attrs to inflate -->
        <item name="android:colorControlNormal">@color/xr_text_primary</item>
        <item name="android:colorControlHighlight">@color/xr_text_primary</item>
        <item name="android:colorAccent">@color/xr_text_primary</item>
        <item name="android:colorControlActivated">@color/xr_text_primary</item>
        <item name="android:colorButtonNormal">@color/xr_text_primary</item>
        <!-- Duplicate non-prefixed color attributes for AppCompat widgets -->
        <item name="colorControlNormal">@color/xr_text_primary</item>
        <item name="colorControlHighlight">@color/xr_text_primary</item>
        <item name="colorAccent">@color/xr_text_primary</item>
        <item name="colorControlActivated">@color/xr_text_primary</item>
        <item name="colorButtonNormal">@color/xr_text_primary</item>
    </style>

    <style name="SettingsTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:colorBackground">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="XrSettingsNavButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">@dimen/xr_space_sm</item>
        <item name="android:background">@drawable/xr_settings_nav_background</item>
        <item name="backgroundTint">@null</item>
        <item name="android:gravity">center_vertical|start</item>
        <item name="android:minHeight">@dimen/xr_control_primary</item>
        <item name="android:paddingStart">@dimen/xr_space_lg</item>
        <item name="android:paddingEnd">@dimen/xr_space_lg</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/xr_text_primary</item>
        <item name="android:textSize">@dimen/xr_text_body</item>
    </style>

    <style name="XrChoiceButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/xr_choice_button_background</item>
        <item name="android:backgroundTint">@null</item>
        <item name="backgroundTint">@null</item>
        <item name="android:elevation">0dp</item>
        <item name="android:gravity">center</item>
        <item name="android:maxLines">2</item>
        <item name="android:minHeight">@dimen/xr_control_primary</item>
        <item name="android:minWidth">76dp</item>
        <item name="android:paddingStart">@dimen/xr_space_lg</item>
        <item name="android:paddingEnd">@dimen/xr_space_lg</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/xr_choice_button_text</item>
        <item name="android:textSize">@dimen/xr_text_title</item>
    </style>

    <style name="XrStepButton" parent="XrChoiceButton">
        <item name="android:minWidth">@dimen/xr_control_standard</item>
        <item name="android:paddingStart">0dp</item>
        <item name="android:paddingEnd">0dp</item>
        <item name="android:textSize">@dimen/xr_text_title</item>
    </style>

    <style name="ExternalDisplayControllerTheme" parent="AppTheme">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <declare-styleable name="ApertureViewGroup">
        <attr name="aperture_color1" format="color" />
        <attr name="aperture_color2" format="color" />
        <attr name="aperture_border_width" format="dimension" />
        <attr name="aperture_border_angle" format="dimension" />
        <attr name="aperture_duration" format="integer" />
        <attr name="aperture_middle_color" format="color" />
    </declare-styleable>

    <declare-styleable name="SeekBarPreference">
        <attr name="xrMin" format="integer" />
        <attr name="xrStep" format="integer" />
        <attr name="xrKeyStep" format="integer" />
        <attr name="xrDivisor" format="integer" />
    </declare-styleable>

</resources>
